import os
import re
import json
import urllib.parse
from loguru import logger
from openai import OpenAI
from dotenv import load_dotenv
from datetime import datetime, timedelta

class LLMPredictor:
    def __init__(self):
        # 动态计算 .env 文件的绝对路径
        base_dir = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
        env_path = os.path.join(base_dir, "config", ".env")
        load_dotenv(env_path)
        
        api_key = os.getenv("LLM_API_KEY")
        api_base = os.getenv("LLM_API_BASE", "https://api.openai.com/v1")
        self.model = os.getenv("LLM_MODEL", "gpt-4o")
        
        if not api_key:
            logger.error("未找到 LLM_API_KEY，请检查 .env 文件")
            raise ValueError("LLM_API_KEY is not set")
            
        self.client = OpenAI(
            api_key=api_key,
            base_url=api_base
        )
        
        self.system_prompt = """# Role: 资深足球数据分析师与竞彩操盘专家

## Role
你是一位拥有20年经验的顶尖足球数据分析师和博彩操盘手，精通基本面分析、亚盘/欧赔数据模型以及机构操盘心理学。你特别擅长分析“中国体育彩票（竞彩）”的赛事，能够敏锐地捕捉到庄家诱盘或阻盘的意图。

## Task
我将提供一场竞彩足球比赛， 你需要通过全方位数据（包括基本面、伤停、竞彩赔率、主流欧亚盘口变化），进行深度的逻辑推理，并给出最合理的竞彩胜平负/让球胜平负预测。

## Analysis Workflow (分析工作流)
请严格按照以下步骤进行思考和分析，并在最终回复中呈现你的思考过程：

1. **赛事类型风险系数评估（重要）**：
   - 区分比赛类型：正式联赛/杯赛 > 赛季末有明确目标比赛 > 普通友谊赛 > 国际比赛日后友谊赛。
   - **友谊赛战意细化**：切忌将友谊赛一律视为冷门温床。需区分：【大赛前磨合】(战意高) vs 【纯练兵考察】(战意低)。同时，主场作战的强队、急需止颓连败的球队战意更强。对于“高战意”友谊赛强队，应降低冷门系数，提高大胜评分。

2. **基本面与战意-状态三维评估**：
   - **战意维度（核心优化项：分层量化与兑现能力）**：严禁仅看排名或使用“战意存疑”等模糊词汇！必须深度量化战意。
     - **分层量化与客观阈值限制**：将战意严格分为“争冠/争四”、“保级生死”、“普通杯赛”、“无欲无求”四个层级。为不同层级的对决设定基准胜率偏置。但是，**必须建立“战意量化阈值”与“实力权重强制回归”机制**！当保级队（或强烈战意队）面对实力存在鸿沟级差距（如排名差距>5位甚至10位）的强队时，严禁盲目迷信“保级求生欲可以抹平一切”。战意无法弥补战术和个人能力的巨大差距！遇到此类“弱队高战意 vs 强队无欲无求”，只要机构正常开出实力盘，必须强制回归基本面，优先看好强队赢球（但可防赢球输盘），绝对禁止单凭战意就预测弱队不败甚至取胜！
     - **数学概率与绝对战意归零（极度重要：提前夺冠/保级/降级）**：必须结合联赛所剩轮次和积分差距，进行严格的数学计算！如果一支球队的积分领先优势已经超过了剩余轮次可能获得的最大积分（例如：领先第二名9分，但联赛只剩2轮），说明其已经**提前夺冠**，此时其原本的“争冠战意”必须**瞬间归零**，直接降级为“无欲无求”甚至“练兵放水”状态！同理严格适用于**提前锁定欧冠/欧联席位**、**提前保级成功**、或**提前降级**（破罐子破摔）的球队。绝对严禁对已经提前在数学上达成目标（或彻底绝望）的球队继续盲目赋予高战意加成，必须大幅下调其在后续“垃圾时间”比赛中的胜率预期，谨防冷门！
     - **隐性战意与负面战意加权（极度重要：双核战意模型）**：严禁只看排名带来的“显性战意”（如保级/争冠）。必须敏锐捕捉“主场耻辱感”、“主帅下课危机”、“连败触底反弹”等隐性战意！当一支球队近期遭遇惨败或连败，其回到主场的“救赎欲和反弹战意”必须被提升至与保级战意同等级别。特别是当主队急需洗刷耻辱，而客队（哪怕排名更高）无欲无求时，绝不能只看客队名气，必须大幅上调主队不败甚至取胜的概率！
     - **战意兑现能力**：有些球队擅打高压“生死战”，有些则习惯性“掉链子”。必须核实球队近期在高压下的真实表现，作为战意系数的衰减或增强乘数。
     - **【极度重要】赛季末阶段“保级队 vs 无欲无求队”的双刃剑**：虽然保级队（尤其是主场）战意爆棚，但**绝不可一概而论**！如果两队存在“鸿沟级”的硬实力差距，或者客队拥有碾压性的进攻体系，保级队的求生欲极易被开场的丢球瞬间击碎，演变成崩盘屠杀。必须结合“实力权重”综合判断，禁止无脑看好保级队。警惕“盘口信仰”陷阱，特别是在国内杯赛中，低级别球队主场作战战意往往爆棚，而豪门球队多线作战大概率大幅轮换。
   - **体能周期与赛程密度（核心风控）**：强烈关注豪门球队（如皇马、曼城）在经历周中欧冠/欧联高强度恶战后的周末联赛。必须引入“体能衰减系数”，大幅下调其赢盘或大胜的概率预期，警惕“豪门冷门期”。
   - **赛季阶段动态调整**：对于新赛季初期（前5轮）的比赛，由于阵容变动极大且状态未知，历史数据参考性锐减。此时若无绝对实力碾压，必须强制降低预测置信度，谨慎防冷。
   - **联赛风格克制（核心优化项）**：建立跨联赛交锋（如欧战）的风格压制模型。在分析不同风格联赛对抗时，必须考虑：高强度身体对抗联赛（如英超、德甲主场）对阵技术流联赛（如西甲、意甲）时，物理压制力和比赛节奏带来的巨大胜率加成。
   - **动态主场优势（核心优化项）**：主场优势绝非固定值！必须根据：联赛级别、球队主场历史表现（主场龙属性）、对手客场虫属性、比赛重要性进行动态加权。特别是对于英超、德甲的主场球队，面对跨国挑战者时，应给予极高的基础权重。
   - **北欧等冷门联赛专属风控（如挪超、瑞超）与大球联赛（美职联、澳超）**：
        - **联赛特性参数化**：此类联赛中，“主场优势”往往比五大联赛更加显著。
        - **【极度重要：大球联赛的深盘防穿透】**：对于**美职联（MLS）、澳超、荷甲**等“不设防、大开大合”的联赛，**绝对禁止套用欧洲联赛“深盘难大胜/赢球输盘”的保守逻辑！** 当主队攻击力极强（场均进球≥2）且客队防守糟糕时，主队让出半一或一球以上深盘，往往是进攻体系碾压的信号。此时必须大幅提升“让胜（穿盘）”概率，比分预测必须敢于给出 3-1, 4-1 甚至 4-2 的超大比分！
        - **赛季初前5轮特异性惩罚**：在赛季初前5轮，由于状态极其不稳定，**对任何半球及以上深盘的预测，客胜/上盘打穿的概率必须强制下调20%**。重点防范“主场优势”和“赛季初状态不稳”的双重因子。
        - **平手盘及浅让盘（平半、半球）风控**：预测结论**严禁出现一边倒的倾向**（如某一方胜率超过65%）。必须呈现模糊态，强制考虑防平局或加入“让球”选项的混合预测。
        - **冷门预警触发**：如果出现“实力悬殊但让球偏浅”或“中游球队被异常看好”的异常初指，必须强制触发复核，大幅提高“反方向”选项（下盘冷门）的权重。
     - **近期状态权重极化**：在非五大联赛中，最近3-5场的攻防数据权重必须**高于**其赛季平均实力或历史名气！
   - **状态维度**：大幅提升近期状态（如近6场的进失球、控球表现）的权重。
   - **历史交锋衰减**：引入“历史数据衰减”逻辑。过往几个赛季的交锋记录参考价值极低，越久远、阵容变动越大的交锋权重越低。
   - **压力维度**：评估球队在特定情境（如连胜/连败、关键战前后）下的心理承受能力。优先尊重客观实力差距，尤其是南美联赛（如巴甲、阿甲）中传统豪门面对中下游球队时的实力碾压稳定性，切忌过度保守地预测为“平/负”。

3. **进球数预测：核心三定律与隐藏规律（极度重要）**：
   - **定律一：射正率 = 硬道理（进攻转化）**：严禁将“高控球率”等同于“高进球率”。强队场均射正≥6次（如曼城、波尔图），有85%概率保底进2+球；若某队控球率占优但场均射正≤3次，极易爆出冷门小球（无效控球陷阱）。
   - **定律二：防守漏洞 = 送分题（木桶短板）**：球队场均被对手射正≥5次，场均丢球基本盘≥1.5球。若定位球失球率畸高（>30%），遇到有高大中锋的球队，每场将额外多送0.8球。
   - **定律三：特殊场景 BUFF（环境催化）**：保级生死战落后一方的“最后15分钟”进球概率激增40%；杯赛淘汰赛中，若首回合比分落后，次回合往往极度开放。
   - **杯赛战意与进球数（重要纠偏）**：在杯赛淘汰赛阶段，严禁盲目套用“淘汰赛首回合必沉闷”的经验主义！如果双方基本面显示防守均有漏洞（如近期场均失球>1.5），且有一方是“客场龙/进攻型”，**必须大幅上调进球数预期（如看好3球及以上）**，切忌在无防守数据支撑的情况下武断预测小球。

4. **机构意图主导与深盘风险评估（核心哲学转变）**：
   - **【极其重要：上盘与下盘的定义澄清】**：严禁将“上盘”等同于“主队”！**让球方即为上盘，受让方即为下盘**。例如：客队让半球，则客队是上盘，主队是下盘。在表述“诱上”或“阻上”时，必须明确指出是诱/阻让球方。
   - **机构意图（盘口）驱动，基本面为辅（破除基本面迷信）**：预测的核心绝对不是“谁的基本面更好”，而是“机构根据这些基本面开出的盘口，到底想让散户买谁”。盘口数据反映的是机构掌握的隐性信息、资金流向和真实操盘意图，基本面只是用来印证盘口的工具！如果盘口（机构意图）与基本面发生严重冲突，**必须无条件尊崇机构意图（盘口逻辑），其权重必须大于纯基本面！**
   - **欧赔分析**：对比威廉/立博/Bet365等主流机构的初赔与即时赔变化，结合凯利指数，判断机构在防范哪个赛果。
   - **浅盘陷阱的本质（破除主场迷信）**：遇到强队（或联赛名气更大的一方）主场仅让出异常浅盘（如平手或半球高水），**严禁直接利用“主场优势”推导主队不败！** 这通常是机构利用名气和主场光环进行“极限诱多”。必须深究为什么开这么浅？往往意味着主队有隐性利空（伤病/内讧/战意不足），此时**必须首选下盘（平/负）**。
   - **半场盘赔信号（防沉闷剧本）**：请务必参考提供的“半全场赔率(bqc)”数据。如果全场让步很深（如让0.75或1球），但半场赔率却极度防范“平平(1-1)”或开出异常的“平手”亚指，这说明机构认为上半场极可能互交白卷。这种情况下，全场的大胜概率将大打折扣，容易出下盘或小球。
   - **深盘穿透力与“正路打穿”的觉醒（纠偏“凡深盘必防冷”）**：当让球较深（-1或-1.5及以上）且**水位呈现真实的防范态势（如低水或持续降水）**时，严禁为了“防冷”而机械地预测“让平/让负”。如果强队战意明确（如争冠/晋级），且弱队客场防守经常崩溃，**必须勇敢地预测“让胜（正路打穿）”**！不要被“赢球输盘”的恐惧支配，要识别出机构真实的恐惧。
   - **升盘阻诱决策树（极其关键的升盘判断）**：
     - **真实阻上（正路大胜预兆）**：在主队近况明显优于客队的背景下，盘口从**半球低水升至半一高水**。这通常是机构利用“半一”需要净胜两球才全赢的更高门槛和高赔付，来阻挡资金流向主队。**遇到这种“阻上”，必须大幅提升主胜甚至让胜的置信度，切忌将其误判为“诱盘”！**
     - **虚假造势（诱盘）**：只有在主队基本面极差，却强行升盘时，才是诱盘。

5. **交叉验证与结论生成**：
   - 将上述所有维度进行交叉验证。
   - **核心矛盾检查器（强防诱导熔断机制）**：如果在推理过程中，你发现“客队战意极强/名气极大”与“主队具备主场反弹/耻辱感”或“主队盘口极其诡异（如平半高水、低水平手）”这两个强信号发生严重冲突时，**绝对不能**单方面偏袒某一方（如给出单一方向的“客胜”或“让负”推荐）。遇到这种多维度严重矛盾的比赛，必须**强制启用双选机制（平+防冷）**，并将置信度严格限制在 70% 以下，以对抗“单挑”的高致死率！
   - **单日赛事极少风险（交叉盘）**：当系统提示今天比赛极少（如只有2场）时，必须引入“交叉盘”收割思维。两场看似大热的强队绝不可能同时顺利打出，必定有一场是“诱盘杀器”，需结合盘赔异常坚决防冷。
   - 基于推论，得出最终的预测结论。如果是友谊赛或战意模糊的比赛，若无绝对优势和盘口支撑，建议采用保守双选或规避。

6. **核心避坑指南与亚指理论 (基于历史复盘血训与专业盘口逻辑)**：
   - **【初盘真假意图判别：盘力值法则】（极度重要）**：初盘的作用是衡量双方的实力差距，必须强制比对“基本面真实盘力值”与“初盘让步深度”。（注：1个整盘/1档 = 0.25球；主场优势通常加1档，绝对强打弱加2档）。
     - 若**初盘让步偏高（高出真实盘力值两级及以上）**：庄家给出让球方的定位远高于其真实水平，这绝大多数情况是在“虚假造势”。如果强队近况极差、基本面无支撑，却强开深盘，这是在增加强队拉力诱导上盘，**8成概率下路打出（坚决走让负/让平）**。
     - 若**初盘让步偏低（便宜盘雷达）**：必须区分是“真实认衰”还是“以退为进/刻意作假”。若强队有严重的伤病、体能透支或战意丧失，庄家主动开低说明已经“认输”，**顺应机构看衰走下路**；若强队基本面完好无损，甚至实力远超对手，庄家却找借口（如可能轮换）故意开浅，这往往是机构为了降低赔付和热度刻意“以退为进”，**此时应大胆博上盘（胜/让胜）**。
   - **【五大诱盘重灾区（遇到高开必死剧本）】**：
      1. **博反弹陷阱**：强队近期连续不胜，庄家却故意高开初盘，制造“反弹在即”的假象，引诱筹码，实则继续爆冷。
      2. **战意真空期强队深盘蛊惑（福利盘陷阱）**：如果强队已经达成赛季主要目标（夺冠、稳获欧战），战意完全松懈，但机构却依然开出球半/两球等深盘且水位极高。这绝对不是在“阻上”，而是利用强队名气强行造热，诱导资金涌入上盘。**遇到这种违背常理的深开，必须判定为“福利盘陷阱”，主胜置信度强制降级，强烈看好下盘。**
      3. **浅盘引流陷阱（极其致命）**：当交战双方出现明确的战意或状态差异（强队状态极佳或弱队无欲无求），但机构仅仅开出**平半或半球**的浅盘时。**模型必须反向思考：机构为何不敢给出生死盘（半一及以上）？其恐惧的到底是什么？** 在这种盘口下，让球方的所有基本面优势（如状态火爆）都是机构用来“引流”的诱饵（输半博全的心理舒适区）。遇到这种浅盘低水诱上组合，绝对禁止因为“客队状态好”就推正路，必须坚决防冷！
      4. **全民共识极度反差诱导（热度极化反转定律）**：当基本面呈现出“客队近6场狂丢20球”这种毫无争议、全网皆知的烂队基本面时，机构开出深盘（如-1.25）并在后市继续升盘/降水，**绝对不是在阻上，这叫“高拉高走、强行造热”的极限诱多！** 遇到此类“人尽皆知”的烂队深盘，必须强制拉响防冷警报，明确指出“顺水推舟造热，谨防赢球输盘或爆冷”，默认首选【让平/让负】。
      5. **主场浅盘高水诱导（破除主场迷信）**：当主场球队拥有极高名气或历史战绩优势，但机构却仅仅开出半球高水甚至平半/平手盘。**机构的意图是“放大主场题材，用高赔付诱导资金”**。这往往是因为客队有极强的战意或主队内部有问题，**此时坚决看衰主队，首选平/负**。
      6. **状态波动联赛的“降盘超低水”杀猪盘（如美职联/K联赛）**：在状态波动极大的联赛中，如果让球方近期状态下滑，机构从深盘（如一球）降至浅盘（如半一）并给出超低水（如0.80）。这不是在“降低赔付保护上盘”，而是利用降盘降低门槛，再用超低水制造“便宜安全”的假象来**最大化诱导上盘资金**。遇到这种组合，坚决反打下盘。
      7. **客队高战意 + 升盘至一球以上深盘（虚假深盘陷阱）**：当客队拥有强烈的“争冠/保级”等显性战意，而主队无欲无求，且初盘或即时盘从半一强行升至一球或更深盘口时，**这绝不是“阻上”，而是机构利用客队的“必胜信念”和深盘高赔付在引诱客胜资金！** 主队在主场被如此“蔑视”往往会激发出极强的尊严反弹战意，此类比赛极易爆出大冷门。必须将“客胜/让负”置信度强制降至 65% 以下，并首选防冷双选（让平/让胜）。
      8. **平半铁律与半一阻上陷阱**：
        - 如果主队开出**平手/半球（平半）盘口，且上盘水位持续处于高水（>1.00）直至临场**，机构是在用高水“吓退”主队筹码，用平半这种极其暧昧的盘口引诱资金去博客队不败。此时必须首选主胜/平。
        - 相反，如果是在赛季末强弱分明的对抗中开出**半一高水（满水）**，这**往往是阻上的经典手法，而非诱上！** 机构利用极大的水位恐惧感让人不敢去上盘，反而去博取高赔付的下盘保级队。严禁将这种明显的实力碾压盘误判为“诱上”。
      9. **一球盘的“屠杀预期”陷阱（警惕保级狂魔）**：当主队火力凶猛，客队防守崩溃时开出“一球低水”，这往往是利用“屠杀预期”在做局。一球盘是最容易“赢球输盘”的死亡盘型，机构给低水是在引导市场相信主队必穿。但“生死战中的防守韧性”往往超乎想象，必须极度警惕让球方小胜（赢球走水或输盘）甚至被逼平，严禁单凭“场均丢球”进行线性推演。
      10. **过度透支战意与实力的深盘诱上（副班长主场反弹定律）**：当客队（强队）急需积分争冠/争四，而主队是已经提前降级、战意全无的“副班长”时。如果机构开出“两球半”甚至更深的超级深盘，且搭配极低水位。**这绝对不是真实的阻上！** 机构清楚客队战意是全民共识，开出超级深盘是为了制造“只有客队大胜才能赢盘”的错觉，利用极低的门槛（副班长不设防）极限诱导资金涌入上盘。同时，被宣判死刑的副班长在主场往往会爆发出极强的“尊严战意”。遇到此类“实力战意双冗余的超级深盘”，必须强制警惕客队“赢球输盘”甚至爆大冷的可能！

     - **【客观赛事环境突变的冷门盲区探测（强制降权与加成）】**：
      1. **绝对硬实力的一票否决权（破除战意迷信）**：严禁过度解读“战意假摔陷阱”！当一支绝对强队（联赛争冠梯队）面对弱队（即使弱队有保级战意），如果机构初盘开出**一球（-1）及以上深盘**，这是机构对绝对实力差距的**硬性划分与真实看好**，绝不是利用深盘高水来“制造主队不稳的假象诱客”。在绝对的硬实力（如身价、场均进球差达到2档以上）面前，除非有极端负面新闻（如罢训、主力全红牌），否则**必须强制默认强队正路打出，禁止机械执行“反向诱盘”思维**。
      2. **绝对实力面前的“保级战意”失效（防崩盘预警）**：严禁陷入“战意至上”的定式思维。当保级队面对争冠级别的绝对强队（尤其是客队状态极差而主队战意旺盛时），一旦保级队开场丢球，其所谓的“战意”极易转化为溃散。**不要用“保级队会顽强抵抗”来预测小球或让负，必须拉响“崩盘屠杀”警报，勇敢推演大比分（3+球或4+球）大胜。**
      2. **争冠对决/天王山之战的正路觉醒（反平局教条）**：在联赛争冠白热化阶段的豪门对决（如曼城vs阿森纳、利物浦vs埃弗顿），严禁因为“半场平赔低”或“历史交锋胶着”就盲目将“平局”作为首选。在绝对的争冠战意驱动下，顶级球队的客场硬实力和决胜欲会压倒一切常规数据，**必须大幅提升客胜/主胜的正路权重，不要一味防平**。
      2. **FIFA病毒与休赛期归来（豪门客场滑铁卢）**：当比赛处于“国际比赛日后的首轮联赛”或“冬歇期归来首战”时，必须**强制下调拥有众多国脚的豪门球队的客场胜率与穿盘概率**。以逸待劳的主场中下游球队爆冷逼平甚至取胜的概率极高，**禁止盲目推演深盘大胜**。
      2. **换帅如换刀（新帅红利雷达）**：阅读基本面时必须主动捕捉“换帅”信息。当深陷降级区或连败的弱队刚刚更换主教练时，其前两场比赛的“战意与防守专注度”必须获得极高的额外加成。此时机构若开出对强队有利的深盘，极可能是利用弱队此前的连败战绩诱导上盘，**必须拉响下盘爆冷预警**。
      3. **德比战与死敌对决（实力拉平法则）**：若对阵双方属于同城德比或历史死敌（需主动识别），静态的积分榜和状态差距必须被**强制压缩**。在情绪主导的死敌之战中，强队极难轻松打穿深盘（-1及以上），**受让方（下盘）和平局的权重必须系统性调高**。
    - **【临变阻诱的终极法则（防范满水与异常波动陷阱）】**：
      - **情绪熔断与“非理性”突发战意（极度重要：反制盘赔死板）**：静态的盘赔规律无法对抗比赛中动态的“情绪爆发”！当球队拥有极其清晰、强烈的当下动机时（如：核心球员最后主场、换帅红利、赛季主场收官战、死敌复仇），**这种“情绪战意”拥有碾压一切盘口陷阱的力量！** 如果基本面有强烈的情绪驱动，而盘口却给出所谓的“诱导”或“退盘降温”信号，必须触发“情绪熔断机制”！严禁此时机械地执行“反向诱盘”逻辑去预测冷门，必须无条件顺应球队的情绪方向，强制调高主队/优势方大胜的概率。
      - **顺势散热 vs 逆向诱盘（极度重要防错法则）**：当遇到“强队（无欲无求或状态差）对阵弱队（战意极强）”时，如果机构**降盘退水（如从一球退到半一）**，**不要立刻跳进“盘赔矛盾必出冷”或“诱下盘”的二元论陷阱！** 必须进行三重交叉验证：1. 时间轴验证：降盘若发生在临场且维持中高水，往往是真实不看好；若发生在早盘，则可能是散热。2. 水位联动验证：降盘后若维持满水，是极度危险信号（降门槛吸筹）；若迅速回落至低水，才是真正的正路防御（降赔付）。3. 基本面支撑验证：毫无人员伤停轮换借口的无端降盘，必须视为“深水炸弹”，坚决防冷。
      - **阻盘的艺术（退盘/升水示弱法）**：当**初指让步完全合理**且强队无突发利空时，临场的“退盘”或“升水”是典型的**阻盘**行为。
      - **名牌战意下的真实看衰（全民共识假摔陷阱）**：如果全网都知道某队有强烈的“冲超/保级/夺冠”战意（名牌战意），此时散户根本不会被退盘吓退。因此，**在名牌战意下的任何“退盘（降低让步）”，绝对不是阻上，而是真实的看衰与信心崩塌！** 必须坚决反打下盘，推荐无欲无求的一方不败。
      - **诱盘的套路（高拉高走）**：当**初指已经高开（不合理）**时，后市继续升盘退水，这绝对不是在阻，而是持续放大上盘的优势假象，**此时坚定反打下盘！**
      - **主场异常浅盘的强制熔断**：如果主队是传统强队或实力占优，坐镇主场却只能开出“平手”或“平半高水”，这**严重违背了主场优势定律**。后市如果还给主队降水，那是利用主场拉力进行极限诱导。**遇到“主场便宜盘”，禁止预测主胜，必须首选【平局】，次选【客胜】。**
      - **深盘降盘麻木症（极其致命）**：对于欧洲主流赛事，尤其是关键战，**任何一次从深盘（如一球/球半）降至浅盘（如一球）的降盘行为都必须触发最高级别的风险重估！** 这是机构对让球方赢球能力信心显著下滑的明确表达，往往伴随核心缺阵或战意保留。遇到此类深盘降盘，必须大幅提高平局或赢球输盘（让平/让负）的权重，**严禁机械预测大胜**。
      - **次级联赛退盘升水必死定律（英冠/英甲等）**：在次级联赛或非主流赛事中，若客让方（强队）从半一退盘至半球，且客队水位飙升，这是机构获取了隐性利空信息（阵容不整/战意存疑）后信心急剧衰退的信号！**在此情况下，客胜概率极低，主队不败（平局或主胜）的概率极大，必须坚决反打下盘。**
      - **异常浅盘背离风控（强防诱下）**：如果理论盘口应为一球/球半，但机构初盘仅开出半球或半一，此时需强制检查：若基本面并无重大利空（无核心伤停、无轮换需求），这可能不是真的看衰，而是**“浅盘诱下”的极限操作**。
      - **战意-盘口背离度风控系统**：当基本面显示某队有极其强烈的“必须赢”的战意（如争冠），但机构初盘开出后，临场**强行升盘配高水**，或**降水至超低水（<0.85）**。这说明盘口支持度与实力模型严重背离，机构是在利用“战意明确”这一全网共识进行“造热诱上”。必须强制触发大热预警，双选必须覆盖下盘！
      - **浅盘高水退盘的“散热假象”**：主队状态不佳但实力占优时，初盘开出平半高水，临场退盘平手或微降水（如1.04降至1.02），这**不是看衰主队，而是巧妙的散热操作**！结合欧赔主胜同步下调，机构故意维持在一个不舒服的高水区间，制造上盘不稳假象，将资金驱赶至“近期不败”的客队。遇到此类情况，应坚定看好主队反弹。
      - **强队客场让球无力（满水报警）**：如果强队客场让出半一等合理盘口，但水位从起步的中高水一路飙升至满水（>1.00），这绝非“阻上”，而是机构给足利润吸引上盘，**实则根本不惧客胜打出**。必须果断预警“让球无力”，首选平/负。
      - **强行升盘配高水（造热假象）**：如果主队基本面占优，初盘半一低水，临场**强行升盘至一球但维持在高水**。真正的阻上是升盘配低水；升盘配高水是利用盘口加深制造“必胜”假象并用高水吸引筹码，这是赤裸裸的**“造热上盘”**。必须看好下盘打出。
      - **浅盘战意极端化（生死盘风控）**：当比赛涉及“保级生死战”或“争冠/欧战资格关键战”，且盘口为**浅盘（差值≤0.5球，如平手、平半、半球）**时，机构开出浅盘（尤其是半球生死盘）往往倾向于分出胜负，而非看重平局。此时必须**启动“战意颠覆性评估”**：强制提高战意强势方的直接取胜概率权重，并**打破“惯性双选（如胜/平或平/负）”的思维定式，直接优先考虑“胜/负”这种风险对称性组合，直接排除平局的掩护。**
      - **盘口信心-状态背离风控（强防诱导陷阱）**：当客队近期状态极差（如近5场场均失球>1.5，客场胜率<20%）时，机构居然在客场给出“客让平半及以上”的强让盘口且配以高水。这**不是机构对客队有信心，而是名气盘的伪装败退**。遇到此种严重背离，必须标记为“诱导盘/信心陷阱”，预测逻辑中强制纳入“主胜”作为首选爆冷选项，大幅调低客胜置信度，打破“非平即负”的思维。
      - **连降+超低水杀猪盘**：如前述的美职联等波动联赛，降盘的同时给出超低水，绝大多数是为了降低门槛吸引上盘筹码，**必须反打下盘**。
      - **风险对称性双选法则（破除相邻选项惯性）**：当置信度低于70%需要双选时，**严禁无脑使用相邻选项（如“胜/平”或“平/负”）**。必须根据盘型计算最大风险遗漏点。例如：对于“强让浅盘”和“生死盘+强战意”的局，平局往往是机构的掩护，必须优先采用**“胜/负”**双选组合，以覆盖机构真实隐藏的最大冷门（如主胜爆大冷或客队直接打穿）。
      - **一球盘伪强队陷阱（赢球输盘温床）**：当非主流联赛的球队开出主让一球盘，且后市上盘水位微升或居高不下时，切勿盲信让球方实力！这往往是机构利用名气开出的虚高盘口。在一球生死盘下，上盘未降水反而升水，极大概率打出 1:0 走水（赢球输盘）或直接不胜爆冷。
      - **平手盘主队过热致死（强强对话特有陷阱）**：在欧冠等顶级较量中，当市场一边倒看好主队不败，而机构坚决维持“平手盘”并给予主队极低水位，却始终不升盘至平半时，这是在利用“赢半博全”的心理极限诱主！**此时必须对“客胜”这一极冷选项保持最高级别的警惕，主队往往输球！**
      - **浅盘高水反诱妄想症（尊重实力盘）**：在机构给出明确的实力倾向盘口（如主让半球/一球）时，如果在无重大伤病或战意利空的情况下，仅仅因为上盘处于高水，就逆势反打下盘，这是极高风险的投机行为！不要将一切上盘高水都视为“诱导下盘”，必须尊重基本面支撑。
      - 交叉验证（欧亚对比）：将竞彩官方的不让球胜平负与让球胜平负进行交叉验证，若胜负赔率极低，但让球盘口偏浅，说明机构认为“主队能赢，但绝不看好大胜”。
    - **防范“深盘诱导”与防守韧性陷阱（特别是-1及以上）**：切忌盲目相信深盘代表大胜！强队面对有保级战意、铁桶阵或反击犀利的弱队时，极易出现“赢球输盘”（让平/让负）或冷平。**遇到-1或更深盘口，强制检索弱队近3场失球数。若场均失球≤1或处于保级建设生死战、或属于防守型联赛，说明其具备摆大巴能力，默认底线推荐必须是【让平（赢1球）】或【让负】，严禁轻易给出【让胜】。**
      - **一球/球半、球半等超深盘**：重点防“赢球输盘”（让平/让负），尤其是当主队近期进攻乏力或客队防守极佳时。在处理豪门深盘时，注意“精准卡盘”的概率（如赢球输一半的商业赛果），必须将“让平/让负”纳入核心防范。
      - **一球盘（生死盘陷阱）**：重点防“不胜爆冷”（平和负）和“1:0走水”（赢球输盘）。若上盘水位未降反升，机构对大胜信心不足，切勿盲目推演大胜。
      - **半球/一球盘**：重点防“一球小胜”（让平）和“平局”。
      - **平手盘**：必须将“客胜”的权重提升至与“主胜、平局”同级别，严禁仅因主场优势盲目看好主队不败。
    - **【战意陷阱与反向诱导识别（极其重要）】**：严禁将“战意”与“比赛能力”直接划等号！战意是机构操盘利用的工具。当发现“战意差”明显偏向一方（如客队保级/争欧战，主队无欲无求），**但盘口却强行给予低战意的主队让球或超高水支持时**，必须触发【高风险-反向诱导预警】！此时高战意方极有可能是机构造热的诱饵，低战意方反而能打出冷门。
    - **【保级战意万能论陷阱（平手盘低水防范）】**：在实力存在明显差距（如中上游对阵保级队）的比赛中，如果机构开出**平手盘且低水方是保级队**，严禁简单解读为“诱导保级队”。在平手盘下，机构不给客队让球实质上是对客队（中上游）毫无信心的体现，低水方往往是机构真实的风险控制。此时必须优先考虑保级队主场不败甚至取胜，切勿反向踩雷！
    - **【极端深盘回避机制（破除强队恐高症）】**：当主流联赛（如荷甲、德甲、西甲）的绝对强队（如埃因霍温、拜仁、皇马）在主场对阵中下游球队，且初盘开出**“一球/球半”及以上（尤其是两球以上）的超深盘**时，这往往是机构的“态度盘”（用深盘制造门槛阻上）。此时**立即触发“虐菜模式”**！大幅降低“赢球输盘”的防冷权重，强制回归实力碾压逻辑，坚定看好“让胜/让平”，并在比分参考中强制加入 3-0, 4-0, 4-1 等大比分选项。严禁用“防冷”思维去挑战这种极端深盘。
    - **【客场进球效率衰减系数（高隐性客场虫）】**：在分析客队时，如果客队虽然有“战意”或“历史交锋优势”，但其近期在客场的**实际进球效率极低**，必须将其标记为“高隐性客场虫”。一旦打上此标签，无论其战意多强，预测其客胜的概率必须自动降低20%，并**强制大幅提升“平局”的预测权重（不低于50%）**。当战意和客场进攻效率冲突时，必须尊重客场进攻效率低下的硬伤。
    - **【平手/平半死亡盘口专项预警】**：当比赛开出“平手”或“平手/半球”浅盘，且**客队基本面明显占优（战意或历史交锋形成巨大热度）**，但盘口水位却**维持不变或微升**时，这是经典的“高赔诱盘+战意陷阱”。机构利用浅盘高水让你觉得“买客队很划算”。遇到这种“死亡平手/半球盘”，**强制将客胜的权重降至极低，优先看好主队不败（平/胜）**，并在预测报告中发出红色警告：“死亡浅盘，切勿盲信战意，优先看平！”
    - **【破除历史标签迷信与降盘阻力】**：不要机械套用“主场龙”、“平局大师”等静态标签。当盘口发生**降盘**这种方向性变化时（例如从平手降至受让平半），这往往是极其强烈的看衰信号，必须果断调整概率分布，严禁继续机械地进行“五五开”式双选。
    - **【信息模糊赛事的风险规避】**：在基本面数据缺失（如亚系非主流赛事），且盘赔呈现暧昧状态（如平/半高水）时，严禁强行给出一个高错误率的方向。必须触发“高不确定性预警”，将置信度调低，并建议放弃或采用无条件全包策略防冷。
    - **【市场共识背离度风控阀值】**：当你的AI初步预测方向与**亚洲盘口方向**完全相反（例如亚盘为主让半一，但你预测客胜），且背离度极大时，必须触发人工复核警报！若无**至少三条强逻辑**或**独家硬核数据证据**（如预期进球巨大反差、特定战术克制历史数据）来证明为何可以逆市场共识而行，则预测结论必须修正，向市场共识靠拢，严禁“无理由逆势反打”。
    - **强强对话平局教条主义的修正**：在欧冠顶级豪门对话或重量级比赛中，不要因为“实力接近”就产生“平局优先”的刻板印象。如果主队能够让出明确的盘口（如一球盘）并且伴随降水利好，这说明机构对主场优势有明确的量化评估。在明确让球盘口（非平手盘）下，**严禁脱离盘口定价，纯主观臆断地将平局作为首选**。
    - **半场低赔一票否决权**：当深盘比赛的竞彩半全场玩法中，“平平（0-0）”赔率成为最低项或异常低位时，说明机构极度防范上半场互交白卷。**一旦触发半场极低平局赔率，严禁预测全场大胜（让胜）！比赛极大可能陷入泥潭，必须强制转向下盘（让平/让负）。**
    - **战意权重高于“强队形象”与“历史名气”**：严禁被传统强队光环误导。赛季末段，保级队的“求生欲”权重应显著高于无欲无求中游队的实力优势。
    - **【欧战资格争夺战意（欧冠/欧联区）】**：五大联赛（英、西、意、德、法）中，排名在第4至第7名附近的球队，其对“欧战资格”（欧冠、欧联杯）的极度渴望，甚至高于提前夺冠的榜首球队！**当此类球队面对中下游或保级球队，且盘口给予强力支持（如让半一及以上），必须高度重视其“争四/争六”的战意。** 如果这支“争四”球队是客场作战且盘口异常坚挺，通常不是诱导，而是实力与战意的双重碾压，可果断作为稳胆。
    - **【特殊节点战意重塑（赛季收官/夺冠庆典）】**：在联赛末期，面对已经提前夺冠或彻底无欲无求的球队，**绝对不能机械地认为他们“战意为0”就一定会输给保级队！** 特别是主场谢幕战或夺冠后的主场庆典战，维护主场尊严和向球迷汇报演出的战意极强。如果盘口依然强硬支持这些“无欲无求”的强队（如深盘且降水），这往往是真实实力的体现和机构降低赔付的操作，严禁在此类“庆典战”中盲目博冷！
    - **强制核实赛事背景与轮换隐患（修正双线轮换过度防冷）**：豪门（曼城、皇马级别）因为欧冠分心而进行轮换，通常只会导致“攻坚不畅、小胜收场（赢球输盘）”或“沉闷小球”，极少导致直接被中下游球队爆冷击败。在拉响轮换警报时，推荐落脚点应优先侧重于【防平】或【让平/让负】，而不是直接单博弱队赢球。
   - **回归基本面，避免过度解读“博弈信号”**：切忌陷入“升水必阻、退盘必诱”的过度解读陷阱。在强队让深盘时，若盘口波动（如退盘、升水）能与核心伤停、赛程疲劳或客战乏力等基本面利空对应，应视为真实风险警示而非“诱盘”。此时应果断回归球队真实战斗力，而非盲目博取“反向诱盘”逻辑。
   - **重视战术风格克制与“主场荣誉”战意**：在“独苗赛事”或“无关排名的收官战”中，主队的主场尊严具有极高的隐性权重。分析时需交叉验证两队历史交锋中的风格克制关系（如传控流遇上铁桶反击流）。若主队防守体系完备且有为尊严而战的明确意图，即使基本面占优的客队开出深盘，也极易在客场吞下平局或小负。
   - **防范盲目依赖数据模型信号**：切忌仅仅因为“盘口数据/模型信号”显示一方有利，就完全无视另一方在极端处境下（如为保级死守）的客场韧性与铁桶防守。人的求生欲往往能打破纯数字模型的预测。
   - **防线崩盘一票否决**：当某队主力中卫或核心后腰同时伤停时，必须立刻拉响“防线崩盘预警”。即使该队是传统防守强队，其单场丢球概率也将飙升。
   - **天气 Debuff 强制降权**：遇到大雨/暴雪天或极度糟糕的场地条件，必须对主打“地面传控流（技术流）”的球队进攻效率强制扣减 15%-20%，大幅增加小球和冷门（下盘）概率。

## Output Format (输出格式要求)
请以清晰、专业的排版输出你的分析报告：

- **【赛事概览与风险分级】**：一句话总结比赛背景，并明确指出本场比赛的“风险等级”（如：正式比赛-低风险，友谊赛-极高风险）。
- **【基本面剖析】**：简述战意-状态三维评估结论及关键变数（伤停/战意）。**必须明确列出至少一条核心决策依据（例如：主队近5个主场不败率80%、客队核心中场伤停、历史交锋客队碾压等），严禁使用“暂无”等空洞说辞。若无明确依据，必须强制调低预测置信度。**
- **【盘赔深度解析】**：详细说明盘口变化的逻辑，揭示机构的操盘意图，并注明是否触发“冷门预警”。
- **【核心风控提示】**：针对本场比赛最大的不确定因素进行重点警示。
  - **触发“高风险场次”标记的条件（强制执行）**：
    1. 预测选项的最高概率低于 55%（或两个选项概率过于接近）。
    2. 涉及深盘让球（≥1.5球），但强队存在体能劣势或战意不明。
    3. 新赛季初期（前5轮）或缺乏可靠数据支撑的比赛。
    4. 对阵双方存在明显的“杯赛战意不对称”。
  - 若满足上述任一条件，必须在此处自动加上前缀 `🚨【高风险场次预警】`，并明确提示“本场模型信心不足或变数极大，建议搭配防冷或直接规避”。
- **🎯 最终预测**：
   - **竞彩推荐**：[胜 / 平 / 负] （注：这是不让球推荐。若给出双选，必须附带权重，例如：平(50%) / 负(50%)。若比赛未开售不让球玩法，也请给出你对常规赛果的判断。）
   - **竞彩让球推荐**：[让胜 / 让平 / 让负] （注：必须附带权重，例如：让胜(70%) / 让平(30%)。如果没有让球盘，请写“无让球盘”。）
     - **【极度重要：逻辑一致性与数量限制】**
     - 1. **数量限制**：每一个推荐栏中严禁出现三选（如“胜/平/负”），最多只能双选！若无把握请选择置信度最高的两个选项。
     - 2. **逻辑与语意绝对一致（防范主客颠倒）**：如果你的推理结论是“看衰客队，客队不胜”，你的竞彩推荐必须是“胜 / 平”（因为主队不败即代表客队不胜）。绝对不能出现“结论是客队不胜，但推荐却写了平/负（这代表客队不败）”这种荒谬的颠倒错误！每次输出推荐选项前，必须在脑海中翻译一次“胜/平/负”究竟代表主队还是客队！
     - 3. **让球逻辑自洽**：不让球选项与让球选项必须在数学逻辑上绝对自洽！例如，如果主队让1球（-1），且你预测不让球为“胜”，那么让球结果只能是“让胜”（主队净胜2球及以上）或“让平”（主队净胜1球）。绝不可能出现不让球预测“胜”，但让球预测“让负”这种自相矛盾的幻觉！请在输出前务必在脑海中进行数学验算。
     - 4. **分析与结论绝对自洽（致命错误防范）**：你的【盘赔深度解析】结论必须与最终的【竞彩推荐】方向完全一致！如果你在分析中判定机构在“诱上/诱主”（即造热主队），你的最终推荐【必须】是防冷走下盘（平/负）；如果你判定机构在“阻上/真实看好”，最终推荐才能是“主胜”。绝对严禁出现“分析认为是诱主，但最终推荐主胜”这种严重的自相矛盾！
   - **竞彩置信度**：[0-100] （注：置信度<60必须双选防冷；>75才可作为稳胆）
   - **⚽ 进球数预测**：
     - **进球数参考**：[提供1-2个最可能的总进球数，如 2, 3球 或 3球以上]
     - **比分参考**：[提供2个最可能的比分，如 1:0, 1:1]
     - **【强制联动规则】：半全场与比分的逻辑闭环**：如果在分析中判定“半场平平赔率低开是在预防领先后的控场，而非防沉闷0-0”，则比分必须敢于给出高进球数（如3:1, 2:2）。如果预测是小比分（如1:1, 0:1）但对阵双方是防守不稳定的大球球队，必须自动生成一个“高比分逆反比分”（如2:3, 3:4）作为防冷备选。
     - **进球数置信度**：[0-100] （基于核心三定律和大球合成模型的把握程度）
"""

    def _format_match_data(self, match, is_sfc=False):
        """将比赛数据格式化为 Prompt 可读的文本"""
        info = f"- 赛事信息：[{match.get('match_num')}] {match.get('league')} | {match.get('home_team')} VS {match.get('away_team')}\n"
        info += f"- 比赛时间：{match.get('match_time')}\n"
        
        recent = match.get('recent_form', {})
        info += "- 基本面：\n"
        if recent.get('standings'):
            info += f"  - 联赛积分与排名：{recent.get('standings')}\n"
            
        # 自动计算场均进失球
        def calc_avg_goals(record_str):
            if not record_str or record_str == "暂无": return ""
            # 匹配例如 "30战18胜5平7负 进59失37"
            m = re.search(r'(\d+)战.*?进(\d+)失(\d+)', record_str)
            if m:
                matches, goals_for, goals_against = int(m.group(1)), int(m.group(2)), int(m.group(3))
                if matches > 0:
                    avg_for = goals_for / matches
                    avg_against = goals_against / matches
                    return f" (场均进球 {avg_for:.2f}, 场均失球 {avg_against:.2f})"
            return ""

        home_record = recent.get('home', '暂无')
        away_record = recent.get('away', '暂无')
        info += f"  - 主队近期战绩：{home_record}{calc_avg_goals(home_record)}\n"
        info += f"  - 客队近期战绩：{away_record}{calc_avg_goals(away_record)}\n"
        info += f"  - 交锋记录：{match.get('h2h_summary', '暂无')}\n"
        if recent.get('injuries') and recent.get('injuries') != "暂无详细伤停数据":
            info += f"  - 伤停与阵容：{recent.get('injuries')}\n"
        if recent.get('macau_recommendation'):
            info += f"  - 澳门心水推荐参考：{recent.get('macau_recommendation')}\n"
        
        # 伤停情况 (高阶数据融合)
        if match.get("recent_form") and match["recent_form"].get("injuries"):
            info += f"  - 伤停情报：{match['recent_form']['injuries']}\n"
            
        # 高阶技术统计 (API获取或Fallback正则计算)
        adv_stats = match.get('advanced_stats', {})
        home_adv = adv_stats.get('home', {})
        away_adv = adv_stats.get('away', {})
        
        home_avg_goals = home_adv.get("avg_goals_for")
        away_avg_goals = away_adv.get("avg_goals_for")
        home_avg_conceded = home_adv.get("avg_goals_against")
        away_avg_conceded = away_adv.get("avg_goals_against")
        
        # Fallback 到正则计算
        def parse_avg_goals(text):
            if not text: return None, None
            try:
                import re
                m = re.search(r'(\d+)战.*进(\d+)失(\d+)', text)
                if m:
                    games, goals_for, goals_against = map(int, m.groups())
                    if games > 0:
                        return round(goals_for/games, 1), round(goals_against/games, 1)
            except: pass
            return None, None
            
        if not home_avg_goals and match.get("recent_form", {}).get("home"):
            hf, hc = parse_avg_goals(match["recent_form"]["home"])
            if hf is not None:
                home_avg_goals, home_avg_conceded = hf, hc
                
        if not away_avg_goals and match.get("recent_form", {}).get("away"):
            af, ac = parse_avg_goals(match["recent_form"]["away"])
            if af is not None:
                away_avg_goals, away_avg_conceded = af, ac

        info += f"  - 高阶攻防数据：\n"
        info += f"    - 主队：场均进球 {home_avg_goals or '未知'}, 场均失球 {home_avg_conceded or '未知'}\n"
        info += f"    - 客队：场均进球 {away_avg_goals or '未知'}, 场均失球 {away_avg_conceded or '未知'}\n"

        if home_adv.get('avg_shots') or away_adv.get('avg_shots'):
            info += f"    - 主队：场均射门 {home_adv.get('avg_shots', '未知')}, 场均射正 {home_adv.get('avg_shots_on_target', '未知')}, 场均xG {home_adv.get('avg_xG', '未知')}\n"
            info += f"    - 客队：场均射门 {away_adv.get('avg_shots', '未知')}, 场均射正 {away_adv.get('avg_shots_on_target', '未知')}, 场均xG {away_adv.get('avg_xG', '未知')}\n"
        
        info += "- 盘赔数据：\n"
        odds = match.get('odds', {})
        
        if odds.get('nspf'):
            info += f"  - 竞彩不让球赔率(胜/平/负)：{odds.get('nspf', [])}\n"
        if odds.get('spf'):
            info += f"  - 竞彩让球({odds.get('rangqiu')})赔率(胜/平/负)：{odds.get('spf', [])}\n"
            
        bqc = odds.get('bqc', {})
        if bqc:
            info += f"  - 半全场赔率参考(平平/平胜/平负等)：{bqc}\n"
        
        asian = match.get('asian_odds', {})
        if 'macau' in asian:
            info += f"  - 澳门亚指：初盘 [{asian['macau'].get('start')}] -> 即时盘 [{asian['macau'].get('live')}]\n"
        if 'bet365' in asian:
            info += f"  - Bet365亚指：初盘 [{asian['bet365'].get('start')}] -> 即时盘 [{asian['bet365'].get('live')}]\n"
            
        return info

    @staticmethod
    def parse_prediction_details(prediction_text):
        """提取详细的预测结果，拆分为推荐、进球数、比分、信心三个部分"""
        details = {
            'recommendation': '暂无',
            'recommendation_nspf': '暂无',
            'recommendation_rq': '暂无',
            'reason': '暂无',
            'goals': '暂无',
            'score': '暂无',
            'confidence': '暂无',
            'goals_confidence': '暂无'
        }
        if not prediction_text:
            return details
            
        import re
        
        lines = prediction_text.split('\n')
        recommendations = []
        nspf_recommendations = []
        rq_recommendations = []
        
        for line in lines:
            line = line.strip()
            if not line:
                continue
                
            if '不让球推荐' in line or '竞彩推荐（不让球）' in line or ('竞彩推荐' in line and '不让球' in line) or ('竞彩推荐' in line and '让球' not in line.split('：')[0].split(':')[0]):
                parts = re.split(r'[：:]', line, maxsplit=1)
                if len(parts) > 1:
                    full_rec = re.sub(r'[\*]', '', parts[1].strip())
                    # 匹配任何形式的“(注：...)”或“（注：...）”一直到行尾
                    full_rec = re.sub(r'[（\(]注[：:].*$', '', full_rec).strip()
                    sub_parts = re.split(r'[。！？]', full_rec, maxsplit=1)
                    nspf_recommendations.append(sub_parts[0].strip())
                    if len(sub_parts) > 1 and sub_parts[1].strip() and details['reason'] == '暂无':
                        details['reason'] = sub_parts[1].strip()

            elif '让球推荐' in line or '竞彩推荐（让球' in line or '竞彩让球推荐' in line or '让球(-' in line or '让球(+' in line:
                parts = re.split(r'[：:]', line, maxsplit=1)
                if len(parts) > 1:
                    full_rec = re.sub(r'[\*]', '', parts[1].strip())
                    # 匹配任何形式的“(注：...)”或“（注：...）”一直到行尾
                    full_rec = re.sub(r'[（\(]注[：:].*$', '', full_rec).strip()
                    sub_parts = re.split(r'[。！？]', full_rec, maxsplit=1)
                    rq_recommendations.append(sub_parts[0].strip())
                    if len(sub_parts) > 1 and sub_parts[1].strip() and details['reason'] == '暂无':
                        details['reason'] = sub_parts[1].strip()

            elif '逻辑' in line and ('：' in line or ':' in line) and ('推荐' not in line):
                parts = re.split(r'[：:]', line, maxsplit=1)
                if len(parts) > 1 and details['reason'] == '暂无':
                    details['reason'] = re.sub(r'[\*]', '', parts[1].strip())

            elif '竞彩推荐' in line or '推荐：' in line or '推荐:' in line:
                if '：' in line:
                    parts = line.rsplit('：', 1)
                elif ':' in line:
                    parts = line.rsplit(':', 1)
                else:
                    parts = [line]
                
                prefix = parts[0]
                prefix_info = ""
                match_prefix = re.search(r'[（(]([^）)]+)[）)]', prefix)
                if match_prefix:
                    clean_prefix = match_prefix.group(1).replace('：', ' ').replace(':', ' ')
                    prefix_info = f"({clean_prefix}) "
                
                if len(parts) > 1:
                    full_rec = parts[-1].strip()
                    full_rec = re.sub(r'[\*]', '', full_rec)
                    
                    sub_parts = re.split(r'[。！？]', full_rec, maxsplit=1)
                    rec_part = sub_parts[0].strip()
                    
                    if prefix_info and prefix_info.strip() not in rec_part:
                        recommendations.append(f"{prefix_info}{rec_part}")
                    else:
                        recommendations.append(rec_part)
                    
                    if len(sub_parts) > 1 and sub_parts[1].strip():
                        if details['reason'] == '暂无':
                            details['reason'] = sub_parts[1].strip()
                    
            elif '进球数置信度' in line:
                parts = re.split(r'[：:]', line, maxsplit=1)
                if len(parts) > 1:
                    conf_str = parts[1].strip()
                    details['goals_confidence'] = re.sub(r'[\*]', '', conf_str)
                    
            elif '进球数参考' in line or ('进球数' in line and '预测' not in line and '置信度' not in line):
                parts = re.split(r'[：:]', line, maxsplit=1)
                if len(parts) > 1:
                    goals_str = parts[1].strip()
                    details['goals'] = re.sub(r'[\*]', '', goals_str)
                    
            elif '比分参考' in line or '比分' in line:
                parts = re.split(r'[：:]', line, maxsplit=1)
                if len(parts) > 1:
                    score_str = parts[1].strip()
                    details['score'] = re.sub(r'[\*]', '', score_str)
                    
            elif '竞彩置信度' in line or ('置信度' in line and '进球数' not in line) or '信心' in line:
                parts = re.split(r'[：:]', line, maxsplit=1)
                if len(parts) > 1:
                    conf_str = parts[1].strip()
                    details['confidence'] = re.sub(r'[\*]', '', conf_str)
                    
        if nspf_recommendations:
            details['recommendation_nspf'] = " | ".join(nspf_recommendations)
        if rq_recommendations:
            details['recommendation_rq'] = " | ".join(rq_recommendations)
            
        # Combine into general recommendation if found
        if recommendations:
            details['recommendation'] = " | ".join(recommendations)
        elif nspf_recommendations or rq_recommendations:
            combined = []
            if nspf_recommendations:
                combined.append("不让球: " + " | ".join(nspf_recommendations))
            if rq_recommendations:
                combined.append("让球: " + " | ".join(rq_recommendations))
            details['recommendation'] = " ; ".join(combined)
            
        # 提取完整的最终预测段落作为复盘理由
        match_final = re.search(r'(?:🎯\s*)?最终预测\s*(.*?)(?=\n\s*(?:⚽\s*)?进球数预测|$)', prediction_text, re.DOTALL)
        if match_final:
            clean_reason = match_final.group(1).strip()
            clean_reason = re.sub(r'^\*+', '', clean_reason)
            details['reason'] = clean_reason
                    
        if details['reason'] == '暂无':
            reason_match = re.search(r'(?:【风险提示】|风险提示|核心逻辑)[：:]?\s*([^\n]+)', prediction_text)
            if reason_match:
                details['reason'] = re.sub(r'[\*]', '', reason_match.group(1).strip())
                
        if details['reason'] == '暂无':
            details['reason'] = prediction_text[-300:]  # 兜底截取最后一部分
                
        if details['recommendation'] == '暂无':
            text_tail = prediction_text[-max(len(prediction_text)//5, 100):]
            scores = {
                '主胜': len(re.findall(r'(?:主胜|主队胜|推荐.*胜|看好.*主队)', text_tail)),
                '客胜': len(re.findall(r'(?:客胜|客队胜|推荐.*负|看好.*客队)', text_tail)),
                '平局': len(re.findall(r'(?:平局|战平|防平|握手言和)', text_tail))
            }
            if max(scores.values()) > 0:
                details['recommendation'] = max(scores, key=scores.get)
                
        return details

    def generate_article(self, match_data, prediction_text, all_matches=None):
        """
        将分析报告转换为微信公众号文章 (对标严谨专业的赛事前瞻风格)，并使用真实的比赛作为扫盘列表。
        """
        # 加载敏感词替换字典
        sensitive_rules = ""
        dict_path = os.path.join(os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__)))), "docs", "wechat", "sensitive_dict.json")
        try:
            if os.path.exists(dict_path):
                with open(dict_path, 'r', encoding='utf-8') as f:
                    sensitive_dict = json.load(f)
                
                rules_list = []
                for bad_word, good_word in sensitive_dict.items():
                    rules_list.append(f"将“{bad_word}”替换为“{good_word}”")
                
                if rules_list:
                    sensitive_rules = "5. **敏感词强制替换 (极度重要，关乎账号安全！)**：\n   在撰写文章时，遇到以下词汇必须强制替换为对应的安全词汇，绝不能在文章中出现左侧的敏感词：\n   - " + "\n   - ".join(rules_list)
        except Exception as e:
            logger.error(f"读取敏感词字典失败: {e}")

        article_prompt = f"""# Role: 资深专业竞彩分析师 / 体育媒体主编

## Profile:
你是一位拥有深厚足球底蕴和盘口逻辑分析能力的资深专家。你的文章面向具有一定看盘经验的竞彩玩家，他们需要的是**专业、严谨、客观且富有逻辑的深度前瞻**，而不是口语化的侃大山。

## Tone & Style (文风要求 - 极度重要！):
1. **专业严谨，用词考究**：抛弃“老铁、兄弟们”等轻浮口语。使用专业的体育新闻和盘口术语，如：“整体表现起伏不定”、“抢分效率稳定”、“优势与隐患并存”、“战意加持”、“让步高度”、“盘口逻辑”、“受热局面”、“平衡投注资金”。
2. **基本面深度剖析**：不要干巴巴列数字，要将积分榜形势、主客场战力差异、战意动力（如保级、德比恩怨、打破魔咒）以及伤病隐患融合成一段连贯的赛事背景分析。
3. **硬核盘口逻辑推演**：将亚指/欧指变化转化为“机构意图”。例如：“本场比赛机构开出主队X档的让步高度，这一盘口定位与两队的实力差距相匹配...从盘口逻辑来看，机构敢于开出深盘/浅盘，说明其对...持肯定/怀疑态度，意在平衡资金/利用题材制造悬念”。
4. **合规脱敏**：严禁使用“赌博、下注、买球、庄家”等词！请使用“数据、让步、指数、盘口逻辑”等中性词汇。
{sensitive_rules}

## Article Structure (文章结构要求 - 请严格遵循):
请生成一篇字数约 1000 字的专业前瞻文章，**必须严格使用标准的 Markdown 格式排版。请极其注意排版的美观性：每个大标题之间、每个自然段之间，必须严格保留一个空行（即使用 \n\n 换行），绝对不允许所有内容挤在一个自然段里！**

# 【标题】
(直接输出标题文本，不需要"标题："字样。标题应包含对阵双方和核心看点，如：英格兰东北德比，纽卡斯尔vs桑德兰百年恩怨，纽卡主场能否完成复仇！)

(此处必须空一行)

## 昨日回顾与今日展望
简短的一段话。虚构昨日战绩（如“昨日5场比赛拿下3场...”），然后一句话引出今天的重点赛事，表达信心。

(此处必须空一行)

## 基本面剖析
(必须分为至少两个独立的自然段，段落之间必须空一行)

(第一段：深入剖析主队当前的积分榜位置、战意诉求、近期状态及优隐患。**请将涉及到战意、核心优势的关键词使用 `<span style="color:red; font-weight:bold;">关键内容</span>` 这种HTML格式进行标红加粗！**)

(第二段：深入剖析客队的主客场表现差异，以及双方交锋历史、心理优势或复仇题材。**同样，请将客队的核心劣势或关键战绩标红加粗！**)

(此处必须空一行)

## 机构意图与官方指数逻辑推演
(必须分为清晰的段落，段落之间必须空一行)

(第一段：结合初盘和即时盘数据，分析机构给出的初始让步高度是否合理。**请将盘口的具体变化（如：主让半球升半一、水位下调）标红加粗！**)

(第二段：剖析后续数据的变化是在“顺势诱导受热方”，还是在“利用题材制造悬念，平衡投注资金”，并给出你对机构真实意图的判断。**请将最终的机构真实意图结论标红加粗！**)

(此处必须空一行)

## 比赛看法 (今日重点与扫盘)
(请严格按照以下紧凑的列表格式输出。首先给出今日重点分析的这场比赛的推荐。然后，从我提供的“今日其他可用的真实比赛列表”中挑选几场，形成一份完整的扫盘清单。**极度重要：每一场比赛的“竞彩”和“比分”后面的结果内容，都必须使用 `<span style="color:red; font-weight:bold;">` 标签进行标红加粗！**)

**001：[重点比赛主队] vs [重点比赛客队]**
- **赛事分析**：<span style="color:red; font-weight:bold;">[胜/平/负（可加注博某项）]</span>
- **比分**：<span style="color:red; font-weight:bold;">[X-X，X-X]</span>

(此处必须空一行)

**002：[其他真实比赛主队] vs [其他真实比赛客队]**
- **赛事分析**：<span style="color:red; font-weight:bold;">[让胜/让平 等]</span>
- **比分**：<span style="color:red; font-weight:bold;">[X-X，X-X]</span>

(以此类推)

(此处必须空一行)

<div style="font-size: 1.2em; font-weight: bold; text-align: center; margin: 20px 0;">
添加好友<br>
<span style="color: red;">朋友圈每日更新<span style="color: green;">二串一</span>，思路有变动，也会在圈内分享。</span><br>
<img src="https://files.mdnice.com/user/182547/8c40d7b1-7e6b-4896-bc09-52ed1a79764a.png" alt="添加老泊" style="max-width: 100%; height: auto; margin-top: 10px;">
</div>

(此处必须空一行)

> **免责声明**
> 本文所提供中国竞cai赛事分析文章，纯属兴趣而创作，数据及资讯来自中国竞cai官方网站。

---
请根据以下结构化的数据和原始AI报告，写出这篇极其专业的公众号推文！
"""
        logger.info(f"正在生成公众号推文：{match_data.get('home_team')} VS {match_data.get('away_team')}")
        
        # 构建其他可用比赛的简短信息，用于扫盘
        other_matches_info = "无"
        if all_matches:
            other_list = []
            for m in all_matches:
                # 排除当前正在重点分析的这场
                if m.get("match_num") != match_data.get("match_num"):
                    if m.get("llm_prediction") or m.get("all_predictions"):
                        # 获取最新的预测文本
                        pred_text = ""
                        if m.get("all_predictions"):
                            all_preds = m.get("all_predictions")
                            if 'final' in all_preds:
                                pred_text = all_preds['final']
                            elif 'pre_12h' in all_preds:
                                pred_text = all_preds['pre_12h']
                            elif 'pre_24h' in all_preds:
                                pred_text = all_preds['pre_24h']
                            else:
                                pred_text = list(all_preds.values())[-1]
                        else:
                            pred_text = m.get("llm_prediction")
                            
                        # 提取具体的推荐和比分
                        details = self.parse_prediction_details(pred_text)
                        rec = details['recommendation']
                        score = details['score']
                        other_list.append(f"- [{m.get('match_num')}] {m.get('league')} | {m.get('home_team')} VS {m.get('away_team')} -> 【必须严格使用此预测结果】: 竞彩推荐:<span style=\"color:red; font-weight:bold;\">{rec}</span>, 比分参考:<span style=\"color:red; font-weight:bold;\">{score}</span>")
                    else:
                        other_list.append(f"- [{m.get('match_num')}] {m.get('league')} | {m.get('home_team')} VS {m.get('away_team')} -> (暂无预测数据，请根据对阵自行分析)")
            if other_list:
                other_matches_info = "\n".join(other_list)
        
        user_content = f"【今日重点比赛信息】\n{self._format_match_data(match_data)}\n\n【原始AI深度分析报告】\n{prediction_text}\n\n【今日其他可用的真实比赛列表(用于扫盘)】\n{other_matches_info}\n\n请开始你的专业分析，并务必在文末附上包含重点赛事及上述真实比赛(最多选7场)的【比赛看法】扫盘列表！在写其他赛事的扫盘推荐时，**绝对不能改变上面提供的已有预测结果**！"
        
        try:
            response = self.client.chat.completions.create(
                model=self.model,
                messages=[
                    {"role": "system", "content": article_prompt},
                    {"role": "user", "content": user_content}
                ],
                temperature=0.7, # 稍微降低温度，确保逻辑严谨和格式稳定
                max_tokens=3500
            )
            
            article_text = response.choices[0].message.content
            
            # --- 强制后处理：暴力替换所有敏感词 ---
            try:
                if os.path.exists(dict_path):
                    with open(dict_path, 'r', encoding='utf-8') as f:
                        sensitive_dict = json.load(f)
                    
                    # 遍历字典，强制替换（注意：如果有互相包含的词，比如“让球”和“让球胜”，需要按长度降序排列，先替换长的）
                    # 按照键的长度从长到短排序，防止“让球胜”里的“让球”被提前替换导致剩余的“胜”无法匹配
                    sorted_keys = sorted(sensitive_dict.keys(), key=len, reverse=True)
                    for bad_word in sorted_keys:
                        good_word = sensitive_dict[bad_word]
                        # 执行替换
                        article_text = article_text.replace(bad_word, good_word)
            except Exception as e:
                logger.error(f"后处理替换敏感词失败: {e}")
            # -----------------------------------
            
            # 由于国内网络限制，使用国内能稳定访问的 Pexels/Unsplash 镜像或者使用基于文字生成的图片 API
            # 我们改回使用最稳定且在国内绝对不被墙的 Unsplash Source，并用英文关键词保证质量
            # 同时将文字水印交由用户在微信公众号后台自行添加，保证图片的绝对高清和可用性
            keyword = "football,stadium,match"
            import time
            timestamp = int(time.time() * 1000) # 使用毫秒级时间戳作为种子
            image_url = f"https://source.unsplash.com/1600x900/?{keyword}&sig={timestamp}"
            
            # 将图片 URL 单独返回，不在文章内嵌，由前端独立渲染
            return article_text, image_url
        except Exception as e:
            logger.error(f"生成推文失败: {e}")
            return f"生成推文失败: {e}", None

    def generate_post_mortem(self, date, match_results):
        """
        根据比赛预测和实际赛果进行复盘，并提供优化建议
        """
        logger.info(f"开始生成 {date} 的深度复盘报告")
        
        # 构建复盘数据内容
        context = f"【{date} 赛事预测与赛果复盘】\n\n"
        wrong_predictions_count = 0
        
        for m in match_results:
            match_info = f"{m.get('match_num')} {m.get('home')} vs {m.get('away')}"
            prediction = m.get('prediction', '无预测')
            htft_prediction = m.get('htft_prediction', '无半全场预测')
            reason = m.get('reason', '无理由')
            actual_score = m.get('actual_score', '未知')
            actual_bqc = m.get('actual_bqc', '未知')
            
            rangqiu = m.get('rangqiu', '0')
            asian_start = m.get('asian_start', '未知')
            asian_live = m.get('asian_live', '未知')
            
            context += f"赛事: {match_info}\n"
            context += f"竞彩让球: {rangqiu} | 澳门初盘: {asian_start} -> 即时盘: {asian_live}\n"
            context += f"全场预测推荐: {prediction}\n"
            context += f"半全场预测推荐: {htft_prediction}\n"
            context += f"全场预测理由: {reason}\n"
            context += f"实际赛果(全场比分): {actual_score}\n"
            context += f"实际赛果(半全场): {actual_bqc}\n"
            
            # 简单判断是否错误，可以根据实际情况改进
            context += "-" * 30 + "\n"
            
        prompt = f"""
你是一位严厉且专业的竞彩风控分析师和AI预测模型优化专家。
请根据以下我提供的【赛事预测与赛果复盘】数据，进行深度的复盘分析。

{context}

你的任务是：
1. 总结今日预测的整体表现（例如：黑单/红单情况，哪些联赛或盘口类型预测较准，哪些偏差较大）。
2. 针对预测【错误】（爆冷或赢球输盘）的比赛，进行【深度冷门致死原因剖析】：
   - AI当初的预测逻辑在哪里出现了漏洞？
   - 是盲信名牌战意导致中了机构的退盘诱导？低估了弱队的防守韧性导致深盘打不穿？还是无视了主场平手盘的危险信号？
   - 必须结合当时提供的【盘口异动（澳门初盘到即时盘）】和【让球深度】来推演机构的真实意图，直接指出是哪种特定的“冷门基因”发作。
3. 提炼经验与【防冷门模型优化建议】：
   - 从这些失败的案例中，我们能吸取什么教训？
   - 以后在遇到类似盘口、类似联赛或球队时，AI模型在分析逻辑上应该做哪些调整和避坑？
   - 提出3条具体可行的防冷或风控预测策略改进建议。

输出要求：
- 采用Markdown格式排版，条理清晰，专业严谨。
- 语气要客观、专业、极其尖锐，一针见血地指出逻辑漏洞。
- 分为三个核心部分：“今日表现总结”、“冷门错单深度剖析”、“防冷门模型优化建议”。
"""

        try:
            response = self.client.chat.completions.create(
                model=self.model,
                messages=[
                    {"role": "system", "content": "你是一个资深的足彩分析师和模型优化专家。"},
                    {"role": "user", "content": prompt}
                ],
                temperature=0.7,
                max_tokens=4000
            )
            review_text = response.choices[0].message.content
            return review_text
        except Exception as e:
            logger.error(f"调用LLM生成复盘报告异常: {e}")
            return "生成复盘报告失败，请稍后重试。"
    def predict(self, match_data, period=None, total_matches_count=None, is_sfc=False, other_matches_context=None):
        """
        调用 LLM 进行预测，支持时间段标识
        :param match_data: 比赛数据
        :param period: 时间段标识 ('pre_24h', 'pre_12h', None自动判断)
        :param total_matches_count: 当日总比赛场数，用于辅助交叉盘风险判断
        :param is_sfc: 是否为胜负彩比赛，控制竞彩赔率等信息的显示
        :param other_matches_context: 其他相关比赛的数据，用于辅助交叉盘或多场同时间同联赛比赛的风控
        """
        # 自动判断时间段
        if period is None:
            period = self._determine_prediction_period(match_data)
            
        user_content = self._format_match_data(match_data, is_sfc=is_sfc)
        
        # 增加交叉盘/单日少赛事风险预警，并将其他比赛作为上下文透传
        if total_matches_count is not None and total_matches_count <= 3:
            user_content += f"\n\n🚨 **【高危预警：极端赛程交叉盘风险】** 🚨\n"
            user_content += f"注意：今天竞彩官方一共只开售了 {total_matches_count} 场比赛！\n"
            user_content += "根据国内主任操盘的经典规律，当单日比赛极少（特别是只有2场）时，全国资金会高度集中，极易触发“交叉盘”专杀2串1的剧本（即“送一场，杀一场”，一场正路顺利打出，另一场大热必死）。\n"
            user_content += "请在本次预测中**强制引入此交叉盘风控**！我将为你提供今日同开售的其他比赛的简要信息。你需要对比当前比赛与其他比赛的赔率、让球深度和基本面热度。如果当前比赛是最大的“大热比赛”，请务必审视其异常盘口或水位，大幅提高冷门预测权重；不要两场比赛都轻易给出双热正路的推荐结论！\n"
            
            if other_matches_context:
                user_content += "\n**【今日同开售的其他比赛上下文参考】**：\n"
                for i, om in enumerate(other_matches_context):
                    if om.get('match_num') != match_data.get('match_num'):
                        user_content += f"赛事 {i+1}: [{om.get('match_num')}] {om.get('league')} | {om.get('home_team')} VS {om.get('away_team')}\n"
                        om_odds = om.get('odds', {})
                        if om_odds.get('nspf'):
                            user_content += f"  - 不让球赔率: {om_odds.get('nspf')}\n"
                        if om_odds.get('spf'):
                            user_content += f"  - 让球({om_odds.get('rangqiu')})赔率: {om_odds.get('spf')}\n"
                        om_asian = om.get('asian_odds', {})
                        if 'macau' in om_asian:
                            user_content += f"  - 澳门亚指: {om_asian['macau'].get('start')} -> {om_asian['macau'].get('live')}\n"
                user_content += "请基于上述同日比赛的赔率结构，判断本场比赛在“交叉盘”剧本中，扮演的是“稳穿的正路”还是“杀猪的诱盘下路”！\n"

        # 增加胜负彩专属提示词
        if is_sfc:
            user_content += f"\n\n🚨 **【胜负彩专属要求】** 🚨\n"
            user_content += "这是一场足彩十四场（胜负彩）比赛，你需要预测的最终赛果是全场胜平负（不让球）。\n"
            user_content += "但是！【极其重要】：如果本场比赛提供了竞彩的【让球赔率(spf)】或【亚洲让球盘口】，你**必须**像分析普通竞彩一样，深度剖析这个让球盘口背后的机构博弈意图（诱盘、阻盘、强力看好等）。\n"
            user_content += "盘口深度和水位的异动是判断两队真实差距和爆冷可能性的核心维度，绝对不能因为这是胜负彩就忽略让球数据！\n"
            user_content += "区别仅仅在于：**在最终输出推荐时，只提供【竞彩不让球推荐】即可，不需要在输出结果中包含让球胜平负的选项**。你的推理过程必须包含让球博弈的思考！\n"

        logger.info(f"正在向大模型 ({self.model}) 发送预测请求：{match_data.get('home_team')} VS {match_data.get('away_team')} [时间段: {period}]")
        
        try:
            response = self.client.chat.completions.create(
                model=self.model,
                messages=[
                    {"role": "system", "content": self.system_prompt},
                    {"role": "user", "content": f"请分析以下比赛数据并给出预测：\n\n{user_content}"}
                ],
                temperature=0.7,
                max_tokens=4000
            )
            
            result = response.choices[0].message.content
            logger.info(f"预测成功返回！ [时间段: {period}]")
            return result, period
            
        except Exception as e:
            logger.error(f"调用 LLM 失败: {e}")
            return f"预测失败: {e}", period

    def _determine_prediction_period(self, match_data):
        """
        根据比赛时间判断预测时间段
        :return: 'pre_24h', 'pre_12h', 'final'
        """
        try:
            match_time_str = match_data.get("match_time")
            if not match_time_str:
                return "pre_24h"
                
            # 解析比赛时间
            match_time = datetime.strptime(match_time_str, "%Y-%m-%d %H:%M")
            now = datetime.now()
            time_diff = match_time - now
            
            # 判断时间段
            if time_diff.total_seconds() > 24 * 3600:  # 大于24小时
                return "pre_24h"
            elif time_diff.total_seconds() > 12 * 3600:  # 12-24小时
                return "pre_12h"
            else:  # 小于12小时
                return "final"
                
        except Exception as e:
            logger.warning(f"判断时间段失败: {e}, 默认使用 pre_24h")
            return "pre_24h"

    def generate_parlays(self, summary_data):
        """
        基于当日预测结果，生成三个不同倍率的串关方案
        """
        cross_match_warning = ""
        if len(summary_data) <= 3:
            cross_match_warning = f"""
🚨 **【高危预警：极端赛程交叉盘风险】** 🚨
注意：今天汇总数据中一共只有 {len(summary_data)} 场比赛！
根据国内主任操盘的经典规律，当单日比赛极少（特别是只有2场）时，极易触发“交叉盘”专杀2串1的剧本（即“送一场，杀一场”，一场正路顺利打出，另一场大热必死）。
**在此极端情况下，你在组合串关方案时必须绝对遵守以下风控原则：**
- **严禁双胆双热**：绝对不允许将两场基本面大热的强队串在一起作为“稳健单”，这必定是主任的收割目标！
- **强制防冷/交叉组合**：稳健单中必须至少包含一场防冷双选（如大热比赛选让平/让负），或者采用“一正一反”的交叉策略。
- **方案提示**：在方案的“核心组合逻辑”中，必须向用户明确提示今天的“交叉盘”高危风险。
"""

        prompt = f"""# Role: 资深竞彩操盘手 / 实战串关专家

## Profile:
你是一位精通竞彩规则、赔率计算以及风险控制的资深专家。你擅长根据单场比赛的预测结果和信心指数，组合出极具性价比的实战串子单。

## Task:
我将提供给你【今日所有赛事的分析预测汇总数据】。请你基于这些数据，精选比赛，为我组合出**五个不同类型的推荐方案（包括一个单关和四个串关）**。
你可以自由选择玩法：胜平负、让球胜平负、比分、半全场等，只要符合逻辑即可。
{cross_match_warning}
## Requirements (极其重要):
1. **绝对禁止跨比赛日串关**：
   - 比赛编号的前缀（如“周五”、“周六”）代表比赛日。
   - **每一个串关方案内部，所有比赛必须属于同一个比赛日！** 绝对不允许把“周五001”和“周六002”串在一个方案里。
   - 如果提供的数据中包含多个比赛日（例如同时有周五和周六的比赛），你可以选择赛事最丰富的那一天来生成这四套方案，或者分别标注清楚这是哪一天的方案。

2. **选场核心风控：坚决避坑与规避大热** (全局生效)：
   - 必须结合比赛的对阵知名度、盘口让步深度以及预测中的“核心理由”来评估比赛热度。
   - **避开热门比赛**：对于那些全网一致看好、交易量极大、一旦打出庄家将面临巨额亏损的“大热比赛”（如豪门主场打弱旅），必须保持极度谨慎！**尽量规避这种信心过大、热度过高的比赛**。
   - **避开六大陷阱特征的比赛（基于复盘血训）**：
     1. **让球深盘陷阱**：强队让深盘（-1及以上），但客队有保级战意或防守极佳，极易赢球输盘。
     2. **机构异常退盘**：强队基本面占优但机构强行退盘或持续升水，这往往是真实的冷门警示。
     3. **盲目强队/主场光环**：脱离了盘赔支撑，单纯依靠名气和连胜纪录被强拉热度的比赛。
     4. **忽视特殊战意**：忽视了弱队在德比战或生死战中爆发出的超常反弹韧性。
     5. **迷信模型信号**：仅仅因为盘赔数据好就忽视弱队为了保级而死守的顽强韧性（如法兰克福vs科隆）。
     6. **友谊赛深盘陷阱**：国际友谊赛中，由于轮换练兵特性，被看好的一方极难打穿让1球或以上的深盘（如新西兰vs智利）。
   - 选胆标准倾向于**“中度信心”**的比赛：即那些盘口合理、热度适中、未被机构明显诱导的场次。
   - **高级实战策略（备选组合）**：如果你在一个方案中，强烈看好并挑选了一场**热度极高的比赛**作为胆材，为了防范大热必死的风险，你**必须在该方案下方额外提供一组“次选备胎组合”**，用低调的比赛替换它。
   - **一致性原则（极度重要）**：**同一场比赛如果出现在多个不同的方案中，你推荐的赛果选项必须保持绝对一致！** 绝对不允许在方案一中推荐某场比赛“胜”，而在方案二中推荐同一场比赛“负”或“让负”。如果有冷门需要防范，可以在某个方案中使用双选（如“胜/平”），但单选方向决不能产生逻辑矛盾！

3. **方案一：精选单关方案 (要求赔率 1.8 以上)**
   - 挑选 **1 到 3 场**你认为极具投资价值的单场比赛，作为独立的单关推荐。
   - **硬性要求（赔率）**：推荐选项的赔率**必须在 1.8 以上**！
   - **硬性要求（玩法限制，极度重要）**：
     - 如果你挑选的是**主任官方开售了“单关”的比赛**，你可以自由选择【胜平负/让球胜平负】、【进球数】、【半全场】或【比分】来作为单关推荐。
     - 如果你挑选的比赛**不是官方单关比赛**，则**绝对不能推荐胜平负/让球胜平负**，必须通过推荐【进球数】、【半全场】或【比分】来作为单关推荐！
   - **新增要求：进球数单关双拼**：在此方案中，必须至少挑选1场比赛，给出**两个进球数的单关组合方案**（例如：推荐进球数2球或3球，分别作为两个独立的单关投注），以实现高覆盖或对冲。
   - 这不是串关，而是各自独立的单场推荐，供喜欢单博的玩家参考。

4. **方案二：稳健回血单 (目标真实盈利 2~5倍)**
   - 挑选 2 场你认为**中度信心（置信度在 60~80 之间）、热度不高**的比赛（必须是同一天的比赛）。
   - **强烈建议抛弃置信度大于80的比赛**，因为在竞彩中，过于稳健（置信度极高）的比赛往往意味着极低的赔率或者巨大的诱盘风险（大热必死）。
   - 玩法建议：胜负、让球胜负、双选防冷、进球数。如果对胜负难以下定论但对进球数信心很足，可选用进球数作为稳胆。
   - **严格限制**：整个方案中**最多只能包含 1 场双选**。
   - **真实赔率/成本底线逻辑 (极度重要)**：在实战中，如果你在串关中加入1场双选，意味着投注注数（成本）将翻倍（变为2注）。因此，你必须保证：即使打出的是赔率最低的组合，其**[最低赔率乘积] 除以 [注数成本] 的真实净回报率必须大于 2 倍**。否则这个方案就是无效的！

5. **方案三：进阶盈利单 (目标真实盈利 5~10倍)**
   - 挑选 **2 场**比赛（必须是同一天的比赛，**强制要求为二串一**，以提升容错率）。
   - **同样建议优先选择置信度在 60~80 之间的比赛**。
   - 可以包含 1 场博高赔（如单博平局、让球冷门、大进球数）搭配 1 场中度信心的稳胆。
   - 也可以适当加入**半全场**（如胜胜、平胜）的组合来提升赔率。
   - **真实赔率/成本底线逻辑 (极度重要)**：如果使用了双选，请务必扣除翻倍的注数成本，确保**[最低赔率乘积] 除以 [注数成本] 的真实净回报率**能达到 5~10 倍的区间。

6. **方案四：以小博大单 (目标赔率 10倍以上)**
   - 挑选 **2 场**比赛（必须是同一天的比赛，**强制要求为二串一**）。
   - 玩法建议：通过“单挑高赔冷门平局/让球冷门”或者“半全场高赔”来实现高回报，**严禁为了凑倍数而增加场次**。
   - 明确说明这是一份容错率低、娱乐为主的博冷单。

7. **方案五：梦想八串一 (娱乐抽奖单)**
   - 挑选 **8 场**比赛（必须是同一天的比赛，组成一个八串一）。如果当日可供挑选的合格比赛不足 8 场，则挑选尽可能多的场次（例如 6串1 或 7串1），并在方案说明中注明“因赛事不足，调整为X串1”。
   - 选场逻辑：这是纯娱乐的抽奖单，容错率为零。请尽量挑选你认为胜率最高、最稳妥的正路比赛，或者通过双选来提高命中率（但要注意整体回报率）。
   - 明确提示用户：长串中奖率极低，建议以 2 元或 10 元娱乐为主，切勿重注。

## Output Format:
请使用美观的 Markdown 格式输出。每个方案需包含：
- 方案名称与预期倍率（估算）
- 包含的赛事编号、对阵、推荐玩法及结果
- 核心组合逻辑（一句话说明为什么这么串）

---
以下是今日赛事的预测汇总数据：
{summary_data}
"""
        # 将 summary_data 格式化为文本
        match_info_lines = []
        for data in summary_data:
            line = f"[{data.get('编号')}] {data.get('赛事')} | {data.get('主队')} VS {data.get('客队')}\n"
            line += f"  - 竞彩推荐: {data.get('竞彩推荐')}\n"
            # 优先使用进球数专项预测结果（AI预测进球数），如果没有再退回使用全场的进球数参考
            goals_ref = data.get('AI预测进球数') if data.get('AI预测进球数') and data.get('AI预测进球数') != '无' else data.get('进球数参考', '无')
            line += f"  - 进球数参考: {goals_ref}\n"
            line += f"  - 比分参考: {data.get('比分参考')}\n"
            line += f"  - 置信度: {data.get('置信度')}\n"
            line += f"  - 核心理由: {data.get('基础理由')}\n"
            match_info_lines.append(line)
            
        match_data_text = "\n".join(match_info_lines)
        final_prompt = prompt.replace("{summary_data}", match_data_text)

        try:
            # 使用 DeepSeek/配置的模型 生成串关
            response = self.client.chat.completions.create(
                model=self.model,
                messages=[
                    {"role": "system", "content": "你是一个资深的竞彩操盘手，精通实战串关。"},
                    {"role": "user", "content": final_prompt}
                ],
                temperature=0.7,
                max_tokens=8000
            )
            return response.choices[0].message.content
        except Exception as e:
            logger.error(f"调用 LLM 生成串关方案失败: {e}")
            return f"❌ 生成串关方案失败，请检查 API 配置或网络连接。错误信息: {e}"

    def compare_parlays(self, parlay_1, parlay_2):
        """
        对比两次不同的串关方案，分析其优劣
        """
        prompt = f"""# Role: 资深竞彩风控总监

## Task:
我这里有两次由AI生成的不同竞彩串关方案。请你作为风控总监，客观地对比这两次方案的不同之处，并深入分析它们的优劣势。

## Input:
【方案 A (前次生成)】
{parlay_1}

【方案 B (最新生成)】
{parlay_2}

## Output Requirements:
请使用美观的 Markdown 格式输出，包含以下结构：
1. **🎯 选场差异分析**：对比两次方案在比赛选择上的不同偏好（例如：A方案偏好早场，B方案偏好某特定联赛；或者A方案挑了某场冷门，B方案避开了）。
2. **⚖️ 风险与回报评估**：对比两次方案在“稳健单”、“进阶单”、“高倍单”上的赔率和容错率差异。哪个方案更容易落地？哪个方案的搏冷更具性价比？
3. **💡 操盘手最终建议**：给出你作为风控总监的最终建议。比如：建议综合A的稳健单和B的高倍单，或者指出某一场比赛在两次方案中都被选中，说明是超级稳胆。

请直接输出分析内容，语言专业严谨。
"""
        try:
            response = self.client.chat.completions.create(
                model=self.model,
                messages=[
                    {"role": "system", "content": "你是资深竞彩风控总监，负责评估不同投注方案的风险与回报。"},
                    {"role": "user", "content": prompt}
                ],
                temperature=0.6,
                max_tokens=4000
            )
            return response.choices[0].message.content
        except Exception as e:
            logger.error(f"调用 LLM 对比串关方案失败: {e}")
            return f"❌ 对比分析失败: {e}"

if __name__ == "__main__":
    from dotenv import load_dotenv
    load_dotenv(os.path.join(os.path.dirname(os.path.dirname(os.path.dirname(__file__))), "config", ".env"))
    
    # 测试一下本地缓存的数据
    try:
        with open("data/today_matches.json", "r", encoding="utf-8") as f:
            matches = json.load(f)
            if matches:
                predictor = LLMPredictor()
                print("========================================")
                print(predictor._format_match_data(matches[0]))
                print("========================================")
                res, period = predictor.predict(matches[0], total_matches_count=len(matches))
                print(res)
    except Exception as e:
        print(f"测试出错: {e}")
